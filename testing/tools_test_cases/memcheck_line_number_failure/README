Example that showed early issues with cuda-memcheck not showing line numbers in code when memory errors detected.

Example is a mixed language code with C++, FORTRAN, and CUDA FORTRAN

-- Aaron Black
